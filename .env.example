# Environment Variables

# Database
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# SSO Configuration (Backend SSO UNSRI as Identity Gateway)
SSO_BASE_URL=http://localhost:8787
SSO_JWKS_URL=http://localhost:8787/.well-known/jwks.json
SSO_ISSUER=http://localhost:8787
SSO_CLIENT_ID=sikp-client
SSO_CLIENT_SECRET=your-client-secret-here
SSO_REDIRECT_URI=http://localhost:5173/callback

# Note: Profile Service should NOT be accessed directly
# All profile data must go through SSO Identity Gateway (/profile endpoint)